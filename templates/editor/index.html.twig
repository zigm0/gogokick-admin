{% extends "editor.html.twig" %}
{% block body %}
  <script>
    window.initialState = {{ initialState|json_encode(constant('JSON_FORCE_OBJECT'))|raw }};
    window.constants    = {{ constants|json_encode|raw }}
  </script>
  <div id="mount" class="h-100"></div>
{% endblock %}
