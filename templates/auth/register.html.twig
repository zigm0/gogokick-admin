{% extends "base.html.twig" %}
{% block body %}
  <h1>Register</h1>

  {{ form_start(form) }}
  {{ form_row(form.email) }}
  {{ form_row(form.plainPassword) }}

  <button class="btn btn-primary" type="submit">
    Register
  </button>
  {{ form_end(form) }}
{% endblock %}
